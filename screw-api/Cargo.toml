[package]
name = "screw-api"
edition = "2024"

[dependencies]
screw-components = { path = "../screw-components" }
screw-core = { path = "../screw-core" }
screw-ws = { path = "../screw-ws", optional = true }
hyper = { version = "1.11.0", features = ["http1", "http2", "server"] }
hyper-util = { version = "0.1.20", features = ["tokio"], optional = true }
http-body-util = { version = "0.1.4", optional = true }
tokio-tungstenite = { version = "0.30.0", optional = true }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = { version = "1.0.151", optional = true }
quick-xml = { version = "0.41.0", features = ["serialize"], optional = true }
futures = { version = "0.3.33", optional = true }
derive-error = { version = "0.0.5", optional = true }

[dev-dependencies]
tokio = { version = "1.53.1", features = ["rt-multi-thread", "macros", "net", "sync"] }
hyper-util = { version = "0.1.20", features = ["tokio"] }

[features]
default = []
ws = ["screw-ws", "tokio-tungstenite", "futures", "hyper-util"]
json = ["derive-error", "serde_json", "http-body-util"]
xml = ["derive-error", "quick-xml", "http-body-util"]

[[test]]
name = "middleware_nesting"
required-features = ["json"]

[[test]]
name = "end_to_end"
required-features = ["json"]

[[example]]
name = "json_api"
required-features = ["json"]

[[example]]
name = "typed_middleware"
required-features = ["json"]

[[example]]
name = "websocket_chat"
required-features = ["json", "ws"]
